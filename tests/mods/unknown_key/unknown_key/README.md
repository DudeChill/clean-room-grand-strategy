Proves a known table with an unknown *key* inside it is reported: the law `mod_typo_law` uses
the modifier key `NoSuchModifier`, which the content loader names in an actionable error
(`unknown modifier 'NoSuchModifier'`) and the validator exits non-zero on.