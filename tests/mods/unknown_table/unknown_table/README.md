Proves unknown content is reported rather than ignored: `common/hovertanks.json` names a table
the engine has no loader for, so the mod is skipped with an actionable error naming the file
and the validator exits non-zero.